/**
 * FILE-02 / D-14: `/model` (no-arg) HTML and `/status` plain text must show the same
 * `providerID/modelID` string for the same resolved OpenCode state.
 */
export const FILE02_D14_MODEL_REF = "anthropic/claude-sonnet-4";
