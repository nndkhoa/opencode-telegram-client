---
phase: 06-power-features
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02-PLAN.md
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - src/bot/handlers/cmd-model.test.ts
  - src/bot/handlers/cmd-status.test.ts
autonomous: true
requirements:
  - FILE-02
  - FILE-03
  - INFRA-03

must_haves:
  truths:
    - "README contains project purpose, install, env table, run — no external links (D-12, D-13)"
    - "REQUIREMENTS and ROADMAP align with photo-only + /new instead of /clear"
    - "FILE-02 regression: /model and /status model line stay consistent or tests document expected behavior"
  artifacts:
    - path: "README.md"
      provides: "INFRA-03 minimal onboarding"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FILE-01/FILE-03 wording aligned with Phase 6"
  key_links:
    - from: "FILE-03"
      to: "/new"
      via: "REQUIREMENTS text"
---

<objective>
Satisfy **INFRA-03** with **minimal README** (**D-12**, **D-13**); align **REQUIREMENTS.md** and **ROADMAP.md** with **06-CONTEXT** (photos only, **FILE-03** via **`/new`**, no **`/clear`**); run **FILE-02** regression (**D-14**) via tests or documented checklist.

Purpose: Docs + traceability closure for Phase 6.

Output: README, updated planning docs, tests or assertions for model/status alignment.
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-power-features/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@README.md
@src/bot/handlers/cmd-model.ts
@src/bot/handlers/cmd-status.ts
</context>

<verification>
- `grep` checks on README
- `npm test` passes
</verification>

<success_criteria>
- `README.md` has `##` heading for installation and env vars without `http` external links (D-13)
- REQUIREMENTS traceability row for Phase 6 matches scope
</success_criteria>

<output>
After completion, create `.planning/phases/06-power-features/06-03-SUMMARY.md`
</output>

<tasks>

<task type="auto">
  <name>Task 1: README.md (minimal onboarding, no external links)</name>
  <files>README.md</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/README.md
    - /Users/admin/repos/opencode-telegram-client/.env.example
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/06-power-features/06-CONTEXT.md
  </read_first>
  <action>
    - Create or replace **`README.md`** content to include: **what the bot is**, **prerequisites** (Node.js, local OpenCode — as plain text, **no URLs** per **D-13**), **`npm install`**, **required environment variables** in a **markdown table** (names matching **`.env.example`**), **how to run** (`npm run dev` or documented script), and **one paragraph** on allowlist behavior without linking to Telegram docs.
    - **Do not** add a **Troubleshooting** section (**D-12**). **Do not** mention **`logs/`** or rotation (**D-11**).
  </action>
  <acceptance_criteria>
    - `test -f README.md`
    - `grep -iE "^## " README.md` shows at least Installation and Configuration or equivalent + Running
    - `test $(grep -c "http" README.md 2>/dev/null || echo 99) -eq 0` exits 0 (no `http`/`https` — **D-13**)
    - `grep -E "TELEGRAM|OPENCODE|ALLOW" README.md` matches variable names from `.env.example` (read `.env.example` for exact names)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test $(grep -c http README.md 2>/dev/null || echo 99) -eq 0'</automated>
  </verify>
  <done>INFRA-03 satisfied with minimal README</done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS.md + ROADMAP.md alignment</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/.planning/REQUIREMENTS.md
    - /Users/admin/repos/opencode-telegram-client/.planning/ROADMAP.md
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/06-power-features/06-CONTEXT.md
  </read_first>
  <action>
    - **FILE-01:** Update acceptance text to **photo** upload to OpenCode (not only “document”) per **D-01**.
    - **FILE-03:** Change to: user clears context via **`/new`** (named session) or equivalent session flow — **not** **`/clear`** (**D-05**).
    - **ROADMAP Phase 6:** Update **goal / success criteria** to match **06-CONTEXT** (photos, structured logging, **no /clear**, README) — remove or rewrite bullets that require **`/clear`** or **document-only** upload if contradicted.
    - **Traceability table:** Ensure Phase 6 rows reflect updated wording.
  </action>
  <acceptance_criteria>
    - `grep -n "FILE-01\|FILE-03" .planning/REQUIREMENTS.md` shows photo and /new wording
    - `grep -n "Phase 6\|Power Features" .planning/ROADMAP.md` shows updated success criteria without mandating `/clear` if CONTEXT forbids it
  </acceptance_criteria>
  <verify>
    <automated>grep -n "FILE-01" .planning/REQUIREMENTS.md && grep -n "Phase 6" .planning/ROADMAP.md</automated>
  </verify>
  <done>Planning docs consistent with user CONTEXT</done>
</task>

<task type="auto">
  <name>Task 3: FILE-02 regression — /model vs /status model display</name>
  <files>src/bot/handlers/cmd-model.test.ts, src/bot/handlers/cmd-status.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/bot/handlers/cmd-model.ts
    - /Users/admin/repos/opencode-telegram-client/src/bot/handlers/cmd-status.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/display-model.ts
  </read_first>
  <action>
    - **D-14:** Add or extend tests so that **mocked** config + session responses produce **the same `providerID/modelID` string** (or documented transformation) in **`/model`** no-arg output and **`/status`** output for the same fixture — e.g. shared helper `formatModelLine` assertion.
    - If tests already exist, **strengthen** with a shared **`describe`** fixture and **grep-friendly** expected substring.
    - Do **not** change production behavior unless a **real mismatch** is found; if found, fix minimal code to align.
  </action>
  <acceptance_criteria>
    - `npm test -- src/bot/handlers/cmd-model.test.ts src/bot/handlers/cmd-status.test.ts` passes
    - Test file contains assertion that both commands reference same model label for mocked data (e.g. `expect(statusText).toContain("anthropic/claude")` and same for model output)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/bot/handlers/cmd-model.test.ts src/bot/handlers/cmd-status.test.ts</automated>
  </verify>
  <done>FILE-02 consistency locked by tests</done>
</task>

</tasks>
