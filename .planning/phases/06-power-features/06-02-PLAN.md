---
phase: 06-power-features
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/opencode/session.ts
  - src/opencode/session.test.ts
  - src/bot/handlers/message.ts
  - src/bot/handlers/photo.ts
  - src/bot/handlers/photo.test.ts
  - src/bot/handlers/unsupported-media.ts
  - src/bot/index.ts
autonomous: true
requirements:
  - FILE-01

must_haves:
  truths:
    - "Telegram photos are downloaded and sent to prompt_async with an image/file part per OpenCode OpenAPI (verified against GET /doc)"
    - "Busy and MCP pending rules match D-15–D-17"
  artifacts:
    - path: "src/bot/handlers/photo.ts"
      provides: "message:photo path with guards"
    - path: "src/opencode/session.ts"
      provides: "prompt with non-text part(s)"
  key_links:
    - from: "src/bot/handlers/photo.ts"
      to: "src/opencode/session.ts"
      via: "sendPromptAsyncWithPhotoBuffer or extended sendPromptAsync"
---

<objective>
Implement **D-01–D-04** and **D-15–D-17**: **`message:photo`** → download → **`POST .../prompt_async`** with parts including an **image/file** part per OpenAPI; **ignore captions**; **reject** photos when busy or when MCP awaits answer; short replies for **non-photo** media (**D-03**).

Purpose: FILE-01 (photos) with shared session and interactive discipline.

Output: Session client extension, photo handler, unsupported-media replies, tests.
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-power-features/06-CONTEXT.md
@.planning/phases/06-power-features/06-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@src/bot/handlers/message.ts
@src/opencode/session.ts
</context>

<verification>
- `npm test` passes
- `npm run typecheck` passes
</verification>

<success_criteria>
- Vitest covers busy guard and MCP-await guard for photo path
- Non-photo handlers return fixed short string containing "not supported" (case-insensitive grep in test)
</success_criteria>

<output>
After completion, create `.planning/phases/06-power-features/06-02-SUMMARY.md`
</output>

<tasks>

<task type="auto">
  <name>Task 1: OpenCode prompt_async with photo bytes (parts schema from GET /doc)</name>
  <files>src/opencode/session.ts, src/opencode/session.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/06-power-features/06-CONTEXT.md
    - /Users/admin/repos/opencode-telegram-client/src/opencode/session.ts
    - /Users/admin/repos/opencode-telegram-client/.planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    - Fetch **`GET http://localhost:4096/doc`** (or project `openCodeUrl` from config in tests use mocked schema) and determine the exact **`parts`** item shape for an **image** (e.g. `type`, `mime`, `filename`, base64 `content` or `url` field). Document the chosen shape in a code comment referencing OpenAPI.
    - Implement **`sendPromptAsyncWithPhoto`** (or extend **`sendPromptAsync`**) that accepts **`Buffer`**, **`mimeType`** (e.g. `image/jpeg`), and optional **empty text omission** — **D-02: do not send caption**; only send the **file/image part** (and no text part unless required by API with empty string — prefer omit text part if OpenCode allows parts-only).
    - Reuse **`getPersistedModelRef` / `model` body** logic from existing **`sendPromptAsync`**.
    - Mock **`fetch`** in **`session.test.ts`** to assert JSON body contains **`parts`** array with **image/file** entry and **no** user caption text.
  </action>
  <acceptance_criteria>
    - `grep -n "sendPromptAsync\|parts" src/opencode/session.ts` shows photo-capable function
    - `grep -n "parts" src/opencode/session.test.ts` shows assertion on multipart or JSON shape
    - `npm test -- src/opencode/session.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/opencode/session.test.ts</automated>
  </verify>
  <done>Server accepts image prompt per verified OpenAPI</done>
</task>

<task type="auto">
  <name>Task 2: Photo handler + unsupported media handlers + registration</name>
  <files>src/bot/handlers/photo.ts, src/bot/handlers/unsupported-media.ts, src/bot/index.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/06-power-features/06-CONTEXT.md
    - /Users/admin/repos/opencode-telegram-client/src/bot/handlers/message.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/interactive-pending.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/streaming-state.ts
  </read_first>
  <action>
    - **`photo.ts`:** For **`message:photo`**: (1) If **`manager.isBusy(chatId)`** → reply **`⏳ Still working on your last message. Please wait.`** (**D-15**). (2) If **`pending.isAwaitingFreeTextAnswer(chatId)`** or pending indicates **active question/permission keyboard** for chat → reply short message: user must **reply with text** or **`/cancel`** (**D-16**, **D-17**). (3) Else **`ctx.getFile()`**, download bytes, infer **`mime`** from `file.file_path` extension or Telegram metadata, call **`sendPromptAsyncWithPhoto`**. (4) **Ignore caption** — do not read `ctx.msg.caption` for OpenCode (**D-02**). (5) Send **`typing`** or **`upload_photo`** chat action as appropriate before long work.
    - **`unsupported-media.ts`:** Register **`message:document`**, **`message:voice`**, **`message:video`**, **`message:sticker`** (and any other easy wins) with the **same allowlist/middleware order** — reply constant short text including **`not supported`** (**D-03**). Do **not** handle **`document`** as upload (**D-01**).
    - **`index.ts`:** Register **`message:photo`** and unsupported handlers **after** commands; **before** or alongside **`message:text`** as grammY requires — ensure **photo** updates are not swallowed by **`message:text`** (photos are not text-only updates).
  </action>
  <acceptance_criteria>
    - `grep -n "message:photo\|message:document" src/bot/index.ts`
    - `grep -n "not supported" src/bot/handlers/unsupported-media.ts`
    - `grep -n "isAwaitingFreeTextAnswer\|isBusy" src/bot/handlers/photo.ts`
    - `npm test` passes including new handler tests with mock context
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Photos flow to OpenCode; other media get short rejection; guards enforced</done>
</task>

</tasks>
