---
phase: 06-power-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/logger.ts
  - .gitignore
  - src/bot/index.ts
  - src/bot/middleware/telegram-log.ts
  - src/bot/middleware/telegram-log.test.ts
  - src/opencode/session.ts
  - src/opencode/config.ts
  - src/opencode/replies.ts
  - src/opencode/health.ts
  - src/opencode/sse.ts
  - src/main.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05

must_haves:
  truths:
    - "Every incoming Telegram update relevant to the bot logs user id, chat id, update type, message id, timestamp at info (LOG-01)"
    - "OpenCode HTTP and SSE emit structured info logs without secrets or full bodies at default info level"
  artifacts:
    - path: "src/logger.ts"
      provides: "pino with stdout + daily-rotating file under logs/"
    - path: "src/bot/middleware/telegram-log.ts"
      provides: "LOG-01 fields from ctx.update"
  key_links:
    - from: "src/opencode/sse.ts"
      to: "LOG-03"
      via: "logger.info per event type + sessionID"
---

<objective>
Implement **D-06–D-10** and **LOG-01–LOG-05**: structured pino logging to **stdout** and **daily-rotating files** under **`logs/`**, with incoming Telegram metadata, OpenCode request/response **metadata** (no full bodies at info), SSE **event type + session id** at info, and error logging — without logging secrets.

Purpose: Observability foundation before photo handler work.

Output: Extended logger, telegram logging middleware, instrumentation in OpenCode modules and SSE loop.
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-power-features/06-CONTEXT.md
@.planning/phases/06-power-features/06-RESEARCH.md
@src/logger.ts
@src/main.ts
@src/opencode/sse.ts
</context>

<verification>
- `npm run typecheck` passes
- `npm test` passes
</verification>

<success_criteria>
- `logs/` directory pattern works; `.gitignore` ignores rotated files
- No bot token or full prompt bodies at default info level
</success_criteria>

<output>
After completion, create `.planning/phases/06-power-features/06-01-SUMMARY.md`
</output>

<tasks>

<task type="auto">
  <name>Task 1: Pino dual output + daily rotation under logs/</name>
  <files>package.json, src/logger.ts, .gitignore</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/logger.ts
    - /Users/admin/repos/opencode-telegram-client/package.json
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/06-power-features/06-CONTEXT.md
  </read_first>
  <action>
    - Add npm dependency **`rotating-file-stream`** (and `@types/rotating-file-stream` if needed for TypeScript).
    - Refactor **`src/logger.ts`** so production and development both write **structured JSON** to a **daily-rotating file** under **`logs/`** (e.g. `logs/app-%Y-%m-%d.log` or library default date pattern), and **also** mirror to stdout. Keep **pino-pretty** only for the stdout stream in non-production if that preserves human-readable console while file stays JSON (**D-10** / **LOG-05**).
    - Add **`logs/`** to **`.gitignore`** (ignore `*.log` or entire `logs/`).
    - Do **not** document logging in README (**D-11**).
  </action>
  <acceptance_criteria>
    - `grep -n "rotating-file-stream\|createStream" package.json src/logger.ts` shows dependency and usage
    - `grep -n "logs" .gitignore` matches log ignore rule
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Logger writes to stdout and rotating file under logs/</done>
</task>

<task type="auto">
  <name>Task 2: LOG-01 middleware — incoming Telegram metadata at info</name>
  <files>src/bot/middleware/telegram-log.ts, src/bot/index.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/bot/index.ts
    - /Users/admin/repos/opencode-telegram-client/src/bot/middleware/allowlist.ts
  </read_first>
  <action>
    - Create **`src/bot/middleware/telegram-log.ts`** exporting middleware that runs **after** allowlist (so only allowed users are logged, matching product scope) and logs at **info** with fields per **D-06**: **`userId`**, **`chatId`**, **`updateType`** (string derived from `ctx.update`: e.g. `message`, `callback_query`, `edited_message`), **`messageId`** (from `message`/`callback_query.message` when present, else omit or null), **`timestamp`** (ISO string from `ctx.update.message?.date` or `Date.now()`).
    - Register **`bot.use(telegramLogMiddleware)`** immediately **after** **`allowlistMiddleware`** in **`createBot`**.
    - Do not log message text bodies at info (**privacy / noise**).
    - Add **`src/bot/middleware/telegram-log.test.ts`** with Vitest: mock **`Context`**, call middleware `next`, assert **`logger.info`** was called with an object containing **`userId`**, **`chatId`**, **`updateType`** (mock `ctx.update` with `message` and `callback_query` cases).
  </action>
  <acceptance_criteria>
    - `grep -n "telegram-log\|telegramLog" src/bot/index.ts`
    - `test -f src/bot/middleware/telegram-log.test.ts`
    - `grep -n "userId" src/bot/middleware/telegram-log.test.ts`
    - `npm test -- src/bot/middleware/telegram-log.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/bot/middleware/telegram-log.test.ts</automated>
  </verify>
  <done>LOG-01 fields emitted at info for allowed updates</done>
</task>

<task type="auto">
  <name>Task 3: LOG-02/03/04 — OpenCode HTTP + SSE info lines</name>
  <files>src/opencode/session.ts, src/opencode/config.ts, src/opencode/replies.ts, src/opencode/health.ts, src/opencode/sse.ts, src/main.ts, src/bot/index.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/opencode/session.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/sse.ts
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/06-power-features/06-CONTEXT.md
  </read_first>
  <action>
    - **HTTP:** After each `fetch` to OpenCode (or in a small shared helper used by these modules), log at **info**: **`method`**, **`path`** (URL pathname), **`sessionId`** when present in path or body context (**D-07**). Do **not** log full JSON bodies at info.
    - **SSE (**`readSseStream` in **`sse.ts`**)**: For **every** parsed event, log at **info** **`eventType`** + **`sessionID`** (from properties when present) per **D-08**. Keep per-delta detail at **debug** only; **`OPENCODE_SSE_VERBOSE`** may remain for deep debugging but must not become default info.
    - **Errors:** On non-ok HTTP or thrown errors in these paths, log at **error** with **`err`** and endpoint context without secrets (**D-09** / **LOG-04**).
    - **`main.ts`**: Remove or downgrade any duplicate noisy `logger.debug` for events if it duplicates new info lines (avoid double-counting).
    - **LOG-04 / Telegram API:** Extend **`createBot`** `bot.catch` handler (or wrap **`bot.api`** calls via grammY **transformer** if preferred) so **failed** `reply` / `editMessage` / **`sendMessage`** log at **error** with **`err`**, **`method`**, and **`chatId`** when available — without token (**D-09**). Minimum: enhance existing **`bot.catch`** in **`index.ts`** to log **`GrammyError`** fields.
  </action>
  <acceptance_criteria>
    - `grep -n "logger.info" src/opencode/sse.ts` includes eventType/sessionID pattern for parsed events
    - `grep -n "logger.info" src/opencode/session.ts` shows post-fetch or path logging for prompt/abort
    - `npm test` passes
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>OpenCode traffic and SSE visible at info without body spam</done>
</task>

</tasks>
