---
phase: 04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/opencode/model-catalog.ts
  - src/opencode/display-model.ts
  - src/opencode/model-catalog.test.ts
  - src/opencode/display-model.test.ts
autonomous: true
requirements:
  - 04.2-D-01
  - 04.2-D-03
  - 04.2-D-04
  - 04.2-D-05
  - 04.2-D-06
must_haves:
  truths:
    - "buildFlatSelectableModelRefs returns the same deterministic order documented in model-catalog.ts"
    - "resolveDisplayModel tries GET /config + extractConfiguredModel before session messages"
    - "resolveDisplayModel uses inner try/catch for getConfig so failures fall through to the message leg"
    - "Unit tests cover config-only, message-only, both agreeing, empty messages + config set, and flat list order"
  artifacts:
    - path: "src/opencode/model-catalog.ts"
      provides: "buildFlatSelectableModelRefs(payload)"
      exports:
        - "buildFlatSelectableModelRefs"
    - path: "src/opencode/display-model.ts"
      provides: "resolveDisplayModel, fetchModelRefFromSessionMessages (or equivalent)"
      exports:
        - "resolveDisplayModel"
    - path: "src/opencode/model-catalog.test.ts"
      provides: "Vitest coverage for flat catalog order"
    - path: "src/opencode/display-model.test.ts"
      provides: "Vitest coverage for D-01 / D-04 resolution matrix"
  key_links:
    - from: "src/opencode/display-model.ts"
      to: "src/opencode/config.ts"
      via: "getConfig, extractConfiguredModel"
      pattern: "getConfig|extractConfiguredModel"
    - from: "src/opencode/model-catalog.ts"
      to: "src/opencode/config.ts"
      via: "ConfigProvidersPayload type"
      pattern: "ConfigProvidersPayload"
---

<objective>
Add pure OpenCode helpers: a deterministic flat catalog of selectable `provider/model` refs (D-05, D-06) and config-first display model resolution (D-01, D-03, D-04) with isolated `getConfig` error handling so handlers can wire in plan 02.

Purpose: Fixes split-brain between listing order and index resolution before Telegram handlers change; enables exhaustive fast unit tests without grammY.

Output: `model-catalog.ts`, `display-model.ts`, and matching Vitest files.
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model/04.2-CONTEXT.md
@.planning/phases/04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model/04.2-RESEARCH.md
@src/opencode/config.ts
@src/bot/handlers/cmd-status.ts
</context>

<interfaces>
From `src/opencode/config.ts`:
```typescript
export type ConfigProvidersPayload = { providers: ProviderInfo[]; default: Record<string, string> };
export function extractConfiguredModel(cfg: unknown): string | undefined;
export async function getConfig(baseUrl: string): Promise<OpenCodeConfig>;
```

New exports (target contracts for plan 02):
```typescript
// model-catalog.ts
export function buildFlatSelectableModelRefs(payload: ConfigProvidersPayload): string[];

// display-model.ts — sessionId undefined skips GET /session/.../message
export type ResolveDisplayModelResult =
  | { kind: "resolved"; ref: string }
  | { kind: "unset" };

export async function resolveDisplayModel(
  baseUrl: string,
  sessionId: string | undefined
): Promise<ResolveDisplayModelResult>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic flat catalog (D-05, D-06)</name>
  <read_first>
    - src/opencode/config.ts (ConfigProvidersPayload, ProviderInfo)
    - src/bot/handlers/cmd-model.ts (formatCatalogBlocks provider sort and fullRef rule)
    - .planning/phases/04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model/04.2-CONTEXT.md (D-05, D-06)
  </read_first>
  <files>src/opencode/model-catalog.ts, src/opencode/model-catalog.test.ts</files>
  <action>
    Create `src/opencode/model-catalog.ts` exporting `buildFlatSelectableModelRefs(payload: ConfigProvidersPayload): string[]`.

    **Ordering (must match D-06 and future numbered `/model` output):**
    1. Sort providers with `(a.name || a.id).localeCompare(b.name || b.id, undefined, { sensitivity: "base" })` (same as `formatCatalogBlocks` in cmd-model.ts).
    2. Within each provider, sort model keys with `Object.keys(models).sort()` (ASCII / UTF-16 code unit order — document in a file-level or function comment).
    3. For each key `id`, full ref: `id.includes("/") ? id : \`${provider.id}/${id}\`` (same as cmd-model).

    Include a short comment block at the top of the function: any change to sort rules must stay in sync with `formatCatalogBlocks` in cmd-model.ts (plan 02 will call both).

    No Telegram or grammY imports.

    Create `src/opencode/model-catalog.test.ts` importing `buildFlatSelectableModelRefs` and a small inline `ConfigProvidersPayload` fixture (≥2 providers, ≥2 models) asserting the returned array equals an explicit ordered `string[]` golden snapshot.
  </action>
  <acceptance_criteria>
    - `rg -n "buildFlatSelectableModelRefs" src/opencode/model-catalog.ts` returns at least one match.
    - `rg -n "localeCompare" src/opencode/model-catalog.ts` matches provider sort.
    - `rg -n "Object\\.keys\\(models\\)\\.sort" src/opencode/model-catalog.ts` matches per-provider key sort.
    - File imports `ConfigProvidersPayload` from `./config.js` (or re-export path consistent with repo ESM `.js` extension).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/opencode/model-catalog.test.ts -x</automated>
  </verify>
  <done>model-catalog.ts exists; model-catalog.test.ts asserts a fixed payload’s flat ref order matches an explicit golden string[] (at least 2 providers, 2+ models).</done>
</task>

<task type="auto">
  <name>Task 2: resolveDisplayModel + unit matrix (D-01, D-03, D-04)</name>
  <read_first>
    - src/opencode/config.ts (getConfig, extractConfiguredModel)
    - src/bot/handlers/cmd-status.ts (lines 7–21: session message scan for assistant + modelID)
    - src/opencode/model-catalog.ts (from Task 1)
    - .planning/phases/04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model/04.2-CONTEXT.md (D-01)
  </read_first>
  <files>src/opencode/display-model.ts, src/opencode/display-model.test.ts</files>
  <action>
    Create `src/opencode/display-model.ts` exporting `ResolveDisplayModelResult` and `resolveDisplayModel(baseUrl, sessionId)`.

    **Resolution order (D-01):**
    1. **Config leg:** In a **nested try/catch**, call `getConfig(baseUrl)` and `extractConfiguredModel(cfg)`. On any throw, `logger.warn` (import from `../logger.js`) with the error — same spirit as cmd-model no-arg path — then continue (do not throw). If a non-empty string is returned, return `{ kind: "resolved", ref }`.
    2. **Message leg:** If `sessionId` is `undefined` or empty, skip fetch and go to step 3. Else `fetch(new URL(\`/session/${sessionId}/message?limit=10\`, baseUrl))`. If not ok or parse fails, treat as no message model. Scan messages for the **first** assistant entry with `info.modelID` (reuse semantics from current `fetchActiveModel` in cmd-status.ts: `providerID ? \`${providerID}/${modelID}\` : modelID`). If found, return `{ kind: "resolved", ref }`.
    3. **Unset:** return `{ kind: "unset" }`.

    Do **not** map missing data to the string `"unknown"` inside this helper — that string is reserved for handler-level “degraded / unreachable” display (plan 02, D-02).

    Create `src/opencode/display-model.test.ts` using `vi.stubGlobal("fetch", ...)` and mocking `getConfig` via `vi.mock("../opencode/config.js", ...)` **or** by stubbing fetch only for `/session/` URLs while importing real getConfig — prefer the same mock style as `cmd-model.test.ts` (mock config module). Cover at minimum:
    - Config-only: getConfig returns model, no session fetch needed.
    - Message-only: getConfig returns empty object, session messages yield assistant ref.
    - Config + message agree: both return same ref.
    - **Empty session + config set (main bug):** messages `[]` or no assistant model, getConfig returns a model → resolved ref from config.
    - getConfig throws: still resolves from messages if session provides model (proves inner catch does not abort whole function).
  </action>
  <acceptance_criteria>
    - `rg -n "resolveDisplayModel" src/opencode/display-model.ts` matches export.
    - `rg -n "kind: \"unset\"" src/opencode/display-model.ts` matches result type.
    - `rg -n "extractConfiguredModel" src/opencode/display-model.ts` matches use.
    - `display-model.test.ts` contains the substring `empty` or `main bug` or a test name describing empty session + config (grep-visible intent).
    - `npx vitest run src/opencode/display-model.test.ts -x` exits 0 after implementation.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/opencode/display-model.test.ts src/opencode/model-catalog.test.ts -x</automated>
  </verify>
  <done>All new unit tests green; resolveDisplayModel implements D-01 ordering with safe getConfig handling.</done>
</task>

</tasks>

<verification>
- `npm test` passes.
- No imports from `grammy` under `src/opencode/` for these files.
</verification>

<success_criteria>
- Golden-order test locks `buildFlatSelectableModelRefs` to documented sort rules.
- `resolveDisplayModel` tests prove config-before-messages and empty-session+config case.
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model/04.2-01-SUMMARY.md`
</output>
