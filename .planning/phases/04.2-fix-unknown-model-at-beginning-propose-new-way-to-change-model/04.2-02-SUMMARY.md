---
phase: 04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model
plan: "02"
subsystem: bot-commands
tags: [status, model, telegram, numbered-catalog]
dependency_graph:
  requires: [04.2-01]
  provides: [status-model-line, model-numeric-switch]
  affects: [src/bot/handlers/cmd-status.ts, src/bot/handlers/cmd-model.ts]
tech_stack:
  added: []
  patterns: [shared-resolveDisplayModel, flat-index-patch]
key_files:
  created: []
  modified:
    - src/bot/handlers/cmd-status.ts
    - src/bot/handlers/cmd-status.test.ts
    - src/bot/handlers/cmd-model.ts
    - src/bot/handlers/cmd-model.test.ts
decisions:
  - "/status healthy+unset shows Model: not set — /model; degraded path keeps Model: unknown"
  - "No-arg /model current line uses (current) when resolveDisplayModel returns resolved; else catalog-default overview"
  - "Digit-only /model arg selects flat[n-1] after getConfigProviders; string args unchanged"
metrics:
  duration: ""
  completed_date: "2026-03-29T00:00:00Z"
  tasks_completed: 2
  files_created: 0
  files_modified: 4
---

# Phase 04.2 Plan 02: /status and /model wiring

**One-liner:** `/status` uses `resolveDisplayModel`; `/model` shows a numbered catalog aligned with the flat list and supports `/model <n>` to PATCH the nth ref.

## What Was Built

- **`cmd-status.ts`** — Removed local session fetch; `resolveDisplayModel` drives the model line; unset includes `/model` hint; unreachable path unchanged.
- **`cmd-model.ts`** — Registry used for session id; numbered catalog via `buildFlatSelectableModelRefs`; numeric branch for `/model <n>`; non-numeric PATCH unchanged.

## Self-Check: PASSED

- `npm test` green; grep expectations in tests cover degraded string and numeric switch.
