# Phase 04.2: Fix unknown model at beginning; propose new way to change model - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `04.2-CONTEXT.md`.

**Date:** 2026-03-29
**Phase:** 04.2 — fix unknown model at beginning; propose new way to change model
**Areas discussed:** `/status` resolution, shared helper, new model-switch UX, copy/hints

---

## Area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Discuss area 1–4 | Cover all proposed gray areas | ✓ |

**User's choice:** User replied **all** to discuss every area.

---

## Area 1 — `/status` model when session has no assistant messages

| Option | Description | Selected |
|--------|-------------|----------|
| A | Config first (`GET /config` + `extractConfiguredModel`), then message-derived model, then unset | ✓ |
| B | Messages first, then config | |
| C | Show config vs session as two fields when they differ | |

**User's choice:** **defaults** → **A**

---

## Area 2 — Single source of truth

| Option | Description | Selected |
|--------|-------------|----------|
| A | Shared helper used by `/status` and `/model` | ✓ |
| B | Duplicate logic | |

**User's choice:** **defaults** → **A**

---

## Area 3 — New way to change model

| Option | Description | Selected |
|--------|-------------|----------|
| A | Status/list consistency only; `/model` stays text `provider/model` only | |
| B | Inline keyboard from `/model` | |
| C | Hybrid | |
| **User extension** | Numbered list in `/model`; `/model <n>` selects by **1-based index** | ✓ |

**User's choice:** Accepted **defaults** for Q1–Q2 and Q4, and specified **numbered `/model` list + `/model <n>`** instead of plain “A only” for area 3. Implementation: digits-only arg → index into deterministic flat list; otherwise existing string `PATCH` behavior.

---

## Area 4 — Copy when model unknown

| Option | Description | Selected |
|--------|-------------|----------|
| A | Short hint to use `/model` when unset | ✓ |
| B | Minimal `unknown` only | |
| C | Verbose second message | |

**User's choice:** **defaults** → **A**

---

## Claude's Discretion

- Exact hint wording for `/status` when model unset
- Exact flat-list ordering implementation details (documented in code)
- Error messages for out-of-range `/model <n>`

## Deferred Ideas

- Inline keyboard picker — deferred
