---
phase: 04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model
plan: 02
type: execute
wave: 2
depends_on:
  - 04.2-01
files_modified:
  - src/bot/handlers/cmd-status.ts
  - src/bot/handlers/cmd-status.test.ts
  - src/bot/handlers/cmd-model.ts
  - src/bot/handlers/cmd-model.test.ts
autonomous: true
requirements:
  - 04.2-D-01
  - 04.2-D-02
  - 04.2-D-03
  - 04.2-D-04
  - 04.2-D-05
  - 04.2-D-06
  - 04.2-D-07
  - 04.2-D-08
  - 04.2-D-09
  - 04.2-D-10
  - FILE-02
  - CMD-05
must_haves:
  truths:
    - "/status shows the globally configured model when the session has no assistant messages yet (same ref as /model would show)"
    - "/status with OpenCode unreachable (checkHealth throws) still shows Model: unknown and State: unknown"
    - "When resolution yields unset, /status includes a short hint mentioning /model"
    - "No-arg /model lists 1-based indices aligned with buildFlatSelectableModelRefs order"
    - "/model &lt;n&gt; with digits-only arg PATCHes the nth flat ref and success line shows the resolved provider/model string"
    - "Non-digit /model args still call patchConfig with the raw string (case-sensitive)"
  artifacts:
    - path: "src/bot/handlers/cmd-status.ts"
      provides: "makeCmdStatusHandler uses resolveDisplayModel"
      contains: "resolveDisplayModel"
    - path: "src/bot/handlers/cmd-model.ts"
      provides: "makeCmdModelHandler uses shared resolver + numeric branch"
      contains: "buildFlatSelectableModelRefs|resolveDisplayModel"
    - path: "src/bot/handlers/cmd-status.test.ts"
      provides: "Regression tests for D-02 and empty session + config"
    - path: "src/bot/handlers/cmd-model.test.ts"
      provides: "Tests for /model 2, out-of-range, resolved ref on success"
  key_links:
    - from: "src/bot/handlers/cmd-status.ts"
      to: "src/opencode/display-model.ts"
      via: "resolveDisplayModel(openCodeUrl, sessionId)"
      pattern: "resolveDisplayModel"
    - from: "src/bot/handlers/cmd-model.ts"
      to: "src/opencode/model-catalog.ts"
      via: "buildFlatSelectableModelRefs before patchConfig for numeric args"
      pattern: "buildFlatSelectableModelRefs|getConfigProviders"
---

<objective>
Wire `resolveDisplayModel` and `buildFlatSelectableModelRefs` into `/status` and `/model`: fix empty-session model line (D-01, D-10), preserve unreachable degradation (D-02), unify current-model display (D-03), extend tests (D-04), and implement numbered catalog + `/model <n>` (D-05–D-09).

Purpose: User-visible fix and faster model switching without typing full refs; FILE-02 + CMD-05 alignment.

Output: Updated cmd-status, cmd-model, and their test files — no bot/index.ts signature change if `makeCmdModelHandler` already receives registry (use non-underscore `registry` and `getActiveSessionId(ctx.chat!.id)` for resolver).
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model/04.2-CONTEXT.md
@src/opencode/display-model.ts
@src/opencode/model-catalog.ts
@src/bot/handlers/cmd-status.ts
@src/bot/handlers/cmd-model.ts
@src/bot/index.ts
</context>

<interfaces>
From plan 04.2-01:
```typescript
import type { ResolveDisplayModelResult } from "../../opencode/display-model.js";
export async function resolveDisplayModel(baseUrl: string, sessionId: string | undefined): Promise<ResolveDisplayModelResult>;
export function buildFlatSelectableModelRefs(payload: ConfigProvidersPayload): string[];
```
From `src/bot/index.ts` (verify): `makeCmdModelHandler(registry, openCodeUrl)` — use registry for `ctx.chat!.id`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: /status wiring (D-01, D-02, D-10) + tests</name>
  <read_first>
    - src/bot/handlers/cmd-status.ts (full file)
    - src/bot/handlers/cmd-status.test.ts (full file)
    - src/opencode/display-model.ts (resolveDisplayModel)
    - src/opencode/health.ts (checkHealth)
  </read_first>
  <files>src/bot/handlers/cmd-status.ts, src/bot/handlers/cmd-status.test.ts</files>
  <action>
    Remove local `fetchActiveModel` from cmd-status.ts; import and `await resolveDisplayModel(openCodeUrl, sessionId)` inside the same `try` block that follows `checkHealth` (keep `checkHealth` as the outer gate for “reachable” vs degraded — D-02).

    **Model line formatting:**
    - If `resolveDisplayModel` returns `{ kind: "resolved", ref }`: set `modelStr = ref` (no `Model: unknown`).
    - If `{ kind: "unset" }`: set `modelStr` to a short label such as `not set` and append a minimal hint per D-10 in the **same reply string**, e.g. `Model: not set — /model` or `Model: not set (use /model)` — keep one line total unless Telegram length forces otherwise.

    Do **not** change degraded branch: when the outer `catch` runs (checkHealth throws), keep exact strings `Model: unknown` and `State: unknown` as today.

    **Tests (cmd-status.test.ts):** Mock `resolveDisplayModel` with `vi.mock("../../opencode/display-model.js", () => ({ resolveDisplayModel: vi.fn() }))` or test through fetch/getConfig if you keep integration style — simplest is mocking the helper. Update tests:
    - Current “healthy + empty messages → Model: unknown” becomes **resolved from config**: mock `resolveDisplayModel` to return `{ kind: "resolved", ref: "anthropic/claude-sonnet-4" }` when simulating the bug fix, OR wire real fetch mocks + getConfig mock via dynamic import — **preferred:** mock `resolveDisplayModel` for precision.
    - Assert degraded output string unchanged when `checkHealth` rejects.
    - Add assertion: when `resolveDisplayModel` returns `unset`, reply includes both `not set` (or chosen label) and `/model` substring (hint).
    - Keep “assistant message” coverage by mocking `resolveDisplayModel` returning resolved ref OR end-to-end mock — align with D-04.

    Update test description comments that reference old D-08/D-09/D-10 IDs if they no longer match 04.2 CONTEXT.
  </action>
  <acceptance_criteria>
    - `rg -n "fetchActiveModel" src/bot/handlers/cmd-status.ts` returns no matches (function removed).
    - `rg -n "resolveDisplayModel" src/bot/handlers/cmd-status.ts` matches import + call.
    - `rg -n "OpenCode: ❌ unreachable \\| Model: unknown \\| State: unknown" src/bot/handlers/cmd-status.test.ts` OR the full expect string for degraded reply still exists in test file.
    - `npx vitest run src/bot/handlers/cmd-status.test.ts -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/bot/handlers/cmd-status.test.ts -x</automated>
  </verify>
  <done>/status uses shared resolver; unreachable path unchanged; unset shows hint with /model.</done>
</task>

<task type="auto">
  <name>Task 2: /model — unified current model, numbered list, /model &lt;n&gt; (D-03, D-05–D-09)</name>
  <read_first>
    - src/bot/handlers/cmd-model.ts (full file)
    - src/bot/handlers/cmd-model.test.ts (full file)
    - src/opencode/model-catalog.ts
    - src/opencode/display-model.ts
    - src/bot/index.ts (makeCmdModelHandler invocation)
  </read_first>
  <files>src/bot/handlers/cmd-model.ts, src/bot/handlers/cmd-model.test.ts</files>
  <action>
    **Registry (D-03):** Change `makeCmdModelHandler(_registry, ...)` to use `registry` and pass `registry.getActiveSessionId(ctx.chat!.id)` into `resolveDisplayModel(openCodeUrl, sessionId)` for the no-arg path’s “Current model” section. Replace the standalone `getConfig` + `extractConfiguredModel` block for overview with logic derived from `resolveDisplayModel` result:
    - `resolved`: show `<code>ref</code>` (and optional `<i>(from config)</i>` only if you can know it came from config without extra calls — **simpler acceptable per discretion:** single line `<code>ref</code> <i>(current)</i>` or reuse prior overview HTML shape but fed from `ref` when resolved; when `unset`, reuse existing `formatModelOverviewHtml` fallback for “not set” / catalog defaults **or** mirror that copy using the same strings as before for unset-only).

    **Catalog display (D-05, D-06):** Import `buildFlatSelectableModelRefs`. In `formatCatalogBlocks`, sort model keys with `Object.keys(models).sort()` before iterating (lock with catalog builder). Prefix each bullet with `n. ` where `n` is 1-based index in the **global** flat list: compute `flat = buildFlatSelectableModelRefs(payload)` once per reply, then when rendering each bullet find the index with `flat.indexOf(fullRef) + 1` **or** iterate `flat` in order and group by provider for display — **simplest consistent approach:** build flat list; iterate providers in sorted order; for each model key sorted, emit `  ${idx}. …` where idx matches position in `flat` (must match `flat[k-1]` for `/model k`).

    **Numeric switch (D-07, D-08, D-09):** For non-empty `arg`:
    - `const trimmed = arg.trim()`
    - If `/^\\d+$/.test(trimmed)`: `n = Number(trimmed)`. Fetch `getConfigProviders(openCodeUrl)`, `flat = buildFlatSelectableModelRefs(payload)`. If `n < 1 || n > flat.length`, reply with a clear error suggesting user run `/model` to see the list (exact wording discretion). Else `const ref = flat[n - 1]`, `await patchConfig(openCodeUrl, ref)`, success: `✅ Model switched to ${ref} (global — affects all sessions).` (show **resolved** ref — D-09).
    - Else: existing `patchConfig(openCodeUrl, trimmed)` path unchanged (D-08, case-sensitive).

    **Imports:** Add logger where needed; avoid duplicate `getConfig` fetch when `resolveDisplayModel` already ran — acceptable to call `resolveDisplayModel` only for overview and use separate `getConfigProviders` for listing/switch (same as today’s pattern).

    **Tests (cmd-model.test.ts):** Add cases: numbered bullets appear in no-arg HTML (e.g. `1.` and `2.`); `ctx.match = "2"` triggers `patchConfig` with second flat ref; out-of-range digit string does not call patchConfig with wrong ref; success message for numeric switch contains full `provider/model` string from sample payload.
  </action>
  <acceptance_criteria>
    - `rg -n "buildFlatSelectableModelRefs" src/bot/handlers/cmd-model.ts` matches.
    - `rg -n "resolveDisplayModel" src/bot/handlers/cmd-model.ts` matches.
    - `rg -n "/^\\\\d+\\$/" src/bot/handlers/cmd-model.ts` OR `test(trimmed)` with digit detection for catalog branch.
    - `rg -n "makeCmdModelHandler\\(" src/bot/index.ts` — registry still passed (verify file if changed).
    - `npx vitest run src/bot/handlers/cmd-model.test.ts -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>/model no-arg shows numbered catalog and current model via resolver; `/model <n>` PATCHes flat ref; string args unchanged.</done>
</task>

</tasks>

<verification>
- Full suite: `npm test`.
- Optional grep: `rg "Model: unknown" src/bot/handlers/cmd-status.ts` only for degraded/unreachable paths, not for healthy+unset if product choice uses “not set”.
</verification>

<success_criteria>
- Phase 04.2 CONTEXT D-01–D-10 satisfied in code + automated tests.
- Manual checks from `04.2-VALIDATION.md` can be run after execute-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model/04.2-02-SUMMARY.md`
</output>
