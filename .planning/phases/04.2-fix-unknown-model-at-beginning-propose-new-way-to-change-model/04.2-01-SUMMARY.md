---
phase: 04.2-fix-unknown-model-at-beginning-propose-new-way-to-change-model
plan: "01"
subsystem: opencode-helpers
tags: [model-catalog, display-model, vitest, config-resolution]
dependency_graph:
  requires: []
  provides: [buildFlatSelectableModelRefs, resolveDisplayModel]
  affects: [src/bot/handlers/cmd-status.ts, src/bot/handlers/cmd-model.ts]
tech_stack:
  added: []
  patterns: [config-first-resolution, deterministic-flat-catalog]
key_files:
  created:
    - src/opencode/model-catalog.ts
    - src/opencode/model-catalog.test.ts
    - src/opencode/display-model.ts
    - src/opencode/display-model.test.ts
  modified: []
decisions:
  - "Flat catalog sort rules mirror formatCatalogBlocks provider order and per-provider Object.keys(models).sort()"
  - "resolveDisplayModel uses one try/catch for getConfig + extractConfiguredModel, then session message leg"
metrics:
  duration: ""
  completed_date: "2026-03-29T00:00:00Z"
  tasks_completed: 2
  files_created: 4
  files_modified: 0
---

# Phase 04.2 Plan 01: Model catalog + display resolution

**One-liner:** Deterministic `buildFlatSelectableModelRefs` and config-first `resolveDisplayModel` (with safe GET /config handling) for handler wiring.

## What Was Built

- **`model-catalog.ts`** — `buildFlatSelectableModelRefs(payload)` returns selectable refs in provider/name order with sorted model keys; golden-order unit test.
- **`display-model.ts`** — `resolveDisplayModel(baseUrl, sessionId)` resolves from GET /config before session messages; returns `unset` when nothing applies; does not emit the string `"unknown"`.

## Self-Check: PASSED

- Key files exist; `npm test` green.
