---
phase: 05-mcp-questions-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/opencode/events.ts
  - src/opencode/events.test.ts
  - src/opencode/interactive-pending.ts
  - src/opencode/interactive-pending.test.ts
  - src/opencode/replies.ts
  - src/opencode/replies.test.ts
autonomous: true
requirements:
  - MCP-03
  - MCP-05
  - MCP-06

must_haves:
  truths:
    - "OpenCodeEvent discriminates question.asked, permission.asked, question.replied, question.rejected with properties-nested sessionID"
    - "POST helpers exist for /question/{requestID}/reply and /permission/{requestID}/reply with correct JSON bodies"
    - "PendingInteractiveState enforces latest prompt wins (D-10), ignores non-active session (D-11), and exposes clear() for lifecycle (MCP-06)"
  artifacts:
    - path: "src/opencode/events.ts"
      provides: "Discriminated unions for interactive SSE events"
    - path: "src/opencode/interactive-pending.ts"
      provides: "Chat-keyed pending state + replace/clear/toggle selection"
    - path: "src/opencode/replies.ts"
      provides: "postQuestionReply, postPermissionReply"
  key_links:
    - from: "src/opencode/replies.ts"
      to: "OpenCode HTTP"
      via: "fetch POST JSON"
      pattern: "/question/.*/reply|/permission/.*/reply"
---

<objective>
Establish typed SSE contracts, in-memory pending interactive state (per chat), and OpenCode HTTP reply clients so later plans can wire Telegram without re-deriving API shapes.

Purpose: Isolates D-10/D-11/D-06 state rules and MCP-03/MCP-05 transport from grammY and main.ts.

Output: Extended `events.ts`, new `interactive-pending.ts` + tests, new `replies.ts` + tests.
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-mcp-questions-permissions/05-CONTEXT.md
@.planning/phases/05-mcp-questions-permissions/05-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@src/opencode/session.ts
</context>

<verification>
- `npx vitest run src/opencode/events.test.ts src/opencode/interactive-pending.test.ts src/opencode/replies.test.ts` passes
- `npm run build` succeeds (if project uses build step)
</verification>

<success_criteria>
- Types and tests cover `question.*` / `permission.*` payload fields needed for UI (validate against `GET /doc` if fields differ — log narrow casts)
- Permission reply body matches D-01: `{ reply: "once" | "always" | "reject" }`
- Question reply sends `{ answers: string[][] }` per OpenAPI (QuestionAnswer array-of-arrays per SDK; adjust if `/doc` differs)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-questions-permissions/05-01-SUMMARY.md`
</output>

<tasks>

<task type="auto">
  <name>Task 1: Discriminated SSE types for question and permission events</name>
  <files>src/opencode/events.ts, src/opencode/events.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/opencode/events.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/streaming-state.ts
    - /Users/admin/repos/opencode-telegram-client/.planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    - Extend `OpenCodeEvent` union with explicit types for at minimum: `question.asked`, `permission.asked`, `question.replied`, `question.rejected`, `permission.replied` (include if ARCHITECTURE lists it), each using `properties: { sessionID: string; ... }` per Phase 2 nested shape.
    - Export type guards or `isQuestionAsked(event)` helpers so `main.ts` can narrow without unsafe casts.
    - Add Vitest fixtures parsing JSON strings into these types; if live schema unknown, use `unknown` fields with documented TODO — prefer zod optional validation only where it reduces crashes (per 05-RESEARCH).
    - Reference D-11: consumers will compare `properties.sessionID` to active session — types must expose `sessionID` on these events.
  </action>
  <acceptance_criteria>
    - `grep -n "question.asked" src/opencode/events.ts` shows union member(s)
    - `grep -n "permission.asked" src/opencode/events.ts` shows union member(s)
    - `npx vitest run src/opencode/events.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/opencode/events.test.ts</automated>
  </verify>
  <done>parseEvent / exported types support interactive events; tests lock shapes</done>
</task>

<task type="auto">
  <name>Task 2: PendingInteractiveState module (latest wins, active session gate, multi-select)</name>
  <files>src/opencode/interactive-pending.ts, src/opencode/interactive-pending.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/session/registry.ts
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/05-mcp-questions-permissions/05-CONTEXT.md
  </read_first>
  <action>
    - Implement `PendingInteractiveState` (or equivalent) keyed by Telegram `chatId` (number), holding: kind (`question` | `permission`), `requestID`, `sessionID`, optional `telegramMessageId` for edit/supersede, multi-select index set per D-06, pagination offset per D-07, short opaque `callbackId` map for 64-byte Telegram limit (Claude’s discretion).
    - **D-10:** `setQuestionAsked` / `setPermissionAsked` replaces any prior pending record for that chat (single authoritative prompt).
    - **D-11:** Add `shouldHandleForChat(chatId, eventSessionId, registry): boolean` — returns true only when `eventSessionId === registry.getActiveSessionId(chatId)`; when false, do not mutate pending (caller logs debug).
    - **MCP-06:** Implement `clear(chatId)`, `clearOnQuestionReplied/Rejected` matching requestID if needed, and document alignment with roadmap (full wiring in 05-03).
    - Expose helpers to register/unregister short callback tokens for question option rows and permission three-way (D-01–D-04).
  </action>
  <acceptance_criteria>
    - `grep -n "PendingInteractiveState\|pendingInteractive" src/opencode/interactive-pending.ts` confirms module name
    - Unit test: two sequential `setQuestionAsked` for same chat → only latest `requestID` stored (D-10)
    - Unit test: `shouldHandleForChat` false when session IDs differ (D-11)
    - `npx vitest run src/opencode/interactive-pending.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/opencode/interactive-pending.test.ts</automated>
  </verify>
  <done>State machine tests green; API ready for bot and SSE layers</done>
</task>

<task type="auto">
  <name>Task 3: OpenCode reply HTTP clients</name>
  <files>src/opencode/replies.ts, src/opencode/replies.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/opencode/session.ts
    - /Users/admin/repos/opencode-telegram-client/.planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    - Add `postQuestionReply(openCodeUrl, requestID, body)` → `POST /question/{requestID}/reply` with JSON `{ answers: ... }` in question order (MCP-03). Match OpenAPI from `/doc` — ARCHITECTURE describes `QuestionAnswer[]`; implement to the installed spec.
    - Add `postPermissionReply(openCodeUrl, requestID, body)` → `POST /permission/{requestID}/reply` with `{ reply: "once" | "always" | "reject", message?: string }` per **D-01–D-04** (MCP-05).
    - Reuse existing fetch patterns from `session.ts` (headers, base URL). No Telegram imports.
    - Tests: mock `global.fetch`, assert URL paths and JSON bodies for one question reply and three permission variants.
  </action>
  <acceptance_criteria>
    - `grep -n "postQuestionReply\|postPermissionReply" src/opencode/replies.ts`
    - `grep -n "once" src/opencode/replies.test.ts` (permission enum covered)
    - `npx vitest run src/opencode/replies.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/opencode/replies.test.ts</automated>
  </verify>
  <done>Both endpoints callable with typed bodies; tests assert payloads</done>
</task>

</tasks>
