---
phase: 05-mcp-questions-permissions
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/main.ts
  - src/bot/index.ts
  - src/bot/handlers/callback-interactive.ts
  - src/bot/handlers/callback-interactive.test.ts
  - src/opencode/interactive-dispatch.ts
  - src/opencode/interactive-dispatch.test.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-04

must_haves:
  truths:
    - "SSE fan-out invokes interactive dispatcher for question/permission events after active-session check (D-11)"
    - "permission.asked shows three inline buttons mapping to once / always / reject (D-01–D-04)"
    - "question.asked with options shows InlineKeyboard per D-05; multi-select and pagination per D-06/D-07"
    - "callback_query handlers answerCallbackQuery and call postQuestionReply / postPermissionReply (MCP-03/05 via 05-01 clients)"
  artifacts:
    - path: "src/main.ts"
      provides: "onEvent branches to StreamingStateManager + interactive dispatch"
    - path: "src/bot/handlers/callback-interactive.ts"
      provides: "grammY callback_query handlers for interactive keyboards"
    - path: "src/opencode/interactive-dispatch.ts"
      provides: "onQuestionAsked / onPermissionAsked → Telegram send/edit"
  key_links:
    - from: "src/main.ts"
      to: "src/opencode/interactive-dispatch.ts"
      via: "onEvent"
      pattern: "handleInteractiveEvent|dispatchInteractive"
    - from: "src/bot/index.ts"
      to: "callback-interactive.ts"
      via: "bot.callbackQuery|bot.on"
      pattern: "callback"
---

<objective>
Wire SSE interactive events to Telegram UI: send or edit messages with inline keyboards for questions (options, multi-select, pagination) and three-button permissions, and handle callback_data with allowlist (ACC-02) and safe answerCallbackQuery.

Purpose: Delivers MCP-01, MCP-02 (plain prompt + `awaitingFreeText` state wired here; submit in 05-03), and MCP-04; pairs with 05-01 state and reply HTTP clients.

Output: `main.ts` dispatch, `interactive-dispatch.ts`, `callback-interactive.ts`, `bot/index.ts` registration.
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mcp-questions-permissions/05-CONTEXT.md
@src/main.ts
@src/bot/index.ts
@src/bot/middleware/allowlist.ts
@src/opencode/interactive-pending.ts
@src/opencode/replies.ts
</context>

<verification>
- `npx vitest run` passes for new handler tests (add minimal mocks)
- Bot starts: `npm run dev` or compile check
</verification>

<success_criteria>
- User sees inline keyboards when OpenCode emits `question.asked` / `permission.asked` for the **active** session (D-11)
- Tapping permission buttons results in correct `postPermissionReply` bodies (D-01)
- Stale prompt from D-10: new SSE prompt replaces pending state before sending new Telegram message (or edit — discretion)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-questions-permissions/05-02-SUMMARY.md`
</output>

<tasks>

<task type="auto">
  <name>Task 1: Interactive dispatch from SSE (session gate + latest wins)</name>
  <files>src/main.ts, src/opencode/interactive-dispatch.ts, src/opencode/interactive-dispatch.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/main.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/streaming-state.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/interactive-pending.ts
    - /Users/admin/repos/opencode-telegram-client/src/session/registry.ts
  </read_first>
  <action>
    - Inject `SessionRegistry`, `PendingInteractiveState`, `openCodeUrl`, and a **chat resolver**: maintain `Map&lt;sessionID, chatId&gt;` updated whenever this bot sends a prompt message for a session (set on first interactive send per plan 05-02 Task 2 — minimally add map APIs on pending state: `rememberSessionChat(sessionID, chatId)` when Telegram message is sent).
    - On SSE `onEvent`: after existing `manager.handleEvent`, if event is `question.asked` or `permission.asked`, resolve `chatId` from `sessionID` map; if no chat mapping yet, skip or log (only chats that have messaged the bot exist — **Claude’s discretion**: iterate registry is not available; session→chat must be populated when user has activity — document that first prompt may require mapping from concurrent message path in 05-03 if gap).
    - **D-11:** Call `pending.shouldHandleForChat(chatId, sessionID, registry)` — if false, `logger.debug` and return.
    - **D-10:** Call pending replace APIs before sending Telegram content.
    - Route to send methods: for `permission.asked`, build three-button keyboard (labels e.g. “Once”, “Always”, “Reject” per D-01 semantics D-02–D-04).
    - For `question.asked` with usable options: build keyboard per D-05; if `multiple: true` per D-06 include toggle rows + Submit; if too large per D-07 paginate with Next/Prev editing same message or fall back to numbered text — implement at least one pagination path and document threshold.
    - For open-ended (no usable inline path): defer full text capture to 05-03 — but send plain text prompt message and set pending mode `awaitingFreeText` on state object so 05-03 can branch (align with MCP-02).
    - Handle `question.replied` / `question.rejected` SSE: call `pending.clear` / clear by requestID per MCP-06 (partial in 05-01; wire here).
  </action>
  <acceptance_criteria>
    - `grep -n "question.asked\|permission.asked" src/main.ts` OR dispatch module shows fan-out
    - `grep -n "getActiveSessionId" src/opencode/interactive-dispatch.ts` for D-11
    - `grep -n "Once\|Always\|Reject\|once" src/opencode/interactive-dispatch.ts` (three-way permission)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/opencode/interactive-dispatch.test.ts</automated>
  </verify>
  <done>SSE path updates Telegram for gated interactive events; permission shows 3 buttons</done>
</task>

<task type="auto">
  <name>Task 2: callback_query handlers + HTTP reply wiring</name>
  <files>src/bot/index.ts, src/bot/handlers/callback-interactive.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/bot/index.ts
    - /Users/admin/repos/opencode-telegram-client/src/bot/middleware/allowlist.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/replies.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/interactive-pending.ts
  </read_first>
  <action>
    - Register **`bot.on("callback_query")`** (or filtered) **after** middleware stack so ACC-02 still applies; for denied users mirror allowlist answer pattern.
    - Parse `ctx.callbackQuery.data` via pending state’s short-token map (64-byte safe); dispatch:
      - Permission: map three callbacks to `postPermissionReply` with `reply: "once" | "always" | "reject"` per **D-01**.
      - Question single-select: `postQuestionReply` with ordered `answers`.
      - Question multi-select: toggle in pending, Submit sends ordered answers per **D-06**.
      - Pagination: Next/Prev updates editMessageText + reply_markup per **D-07**.
    - Always `await ctx.answerCallbackQuery()` in `try/finally` (ACC-02 / UX).
    - On successful POST, clear pending for that chat for that request (or full clear per MCP-06 rules).
    - Pass dependencies: `PendingInteractiveState`, `openCodeUrl`, `SessionRegistry` if needed for validation.
  </action>
  <acceptance_criteria>
    - `grep -n "callback_query" src/bot/index.ts`
    - `grep -n "answerCallbackQuery" src/bot/handlers/callback-interactive.ts`
    - `grep -n "postPermissionReply\|postQuestionReply" src/bot/handlers/callback-interactive.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/bot/handlers/callback-interactive.test.ts</automated>
  </verify>
  <done>Callbacks resolve to OpenCode replies with correct JSON; spinners cleared</done>
</task>

</tasks>
