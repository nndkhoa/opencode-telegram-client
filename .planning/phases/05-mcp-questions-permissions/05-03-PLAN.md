---
phase: 05-mcp-questions-permissions
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/bot/handlers/message.ts
  - src/bot/handlers/cmd-cancel.ts
  - src/bot/handlers/cmd-switch.ts
  - src/bot/handlers/cmd-new.ts
  - src/session/session-chat-map.ts
  - src/main.ts
  - src/bot/handlers/message.test.ts
  - src/bot/handlers/cmd-cancel.test.ts
  - src/bot/handlers/cmd-switch.test.ts
  - src/bot/handlers/cmd-new.test.ts
autonomous: true
requirements:
  - MCP-02
  - MCP-06

must_haves:
  truths:
    - "Open-ended question path: bot prompts user; next non-command text submits postQuestionReply (D-08, D-09, MCP-02)"
    - "Pending cleared on /cancel, /switch, /new (session change), question.replied/rejected SSE (MCP-06)"
    - "Commands stay ahead of free-text answer capture (grammY order unchanged)"
  artifacts:
    - path: "src/bot/handlers/message.ts"
      provides: "Branch: awaiting answer vs normal prompt"
    - path: "src/bot/handlers/cmd-cancel.ts"
      provides: "Clears pending interactive + existing abort"
  key_links:
    - from: "src/bot/handlers/message.ts"
      to: "src/opencode/replies.ts"
      via: "postQuestionReply when awaiting"
      pattern: "awaiting|pendingInteractive"
    - from: "src/bot/handlers/cmd-switch.ts"
      to: "PendingInteractiveState"
      via: "clear on switch"
      pattern: "clear\\("
---

<objective>
Complete MCP-02 free-text path and MCP-06 lifecycle: message handler submits open-ended answers without stealing commands; session-changing and cancel commands clear pending state; consolidate sessionID↔chatId indexing if not finished in 05-02.

Purpose: Satisfies roadmap success criteria 2 and 4; closes D-08/D-09 integration.

Output: Updated message + command handlers, tests for precedence and clears.
</objective>

<execution_context>
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/admin/repos/opencode-telegram-client/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mcp-questions-permissions/05-CONTEXT.md
@src/bot/handlers/message.ts
@src/bot/handlers/cmd-cancel.ts
@src/bot/handlers/cmd-switch.ts
@src/bot/handlers/cmd-new.ts
@src/opencode/interactive-pending.ts
</context>

<verification>
- `npx vitest run` full suite green
- Grep checks below in acceptance criteria
</verification>

<success_criteria>
- While awaiting free-text, `/cancel` runs cancel handler and clears pending (not submitted as answer)
- While awaiting, `/switch foo` runs switch and clears pending per MCP-06
- `question.replied` / `question.rejected` from SSE clears pending (if not already cleared in 05-02, ensure single source of truth)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-questions-permissions/05-03-SUMMARY.md`
</output>

<tasks>

<task type="auto">
  <name>Task 1: sessionID ↔ chatId index + message handler awaiting branch</name>
  <files>src/session/session-chat-map.ts, src/bot/handlers/message.ts, src/main.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/bot/handlers/message.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/interactive-pending.ts
    - /Users/admin/repos/opencode-telegram-client/src/opencode/interactive-dispatch.ts
  </read_first>
  <action>
    - If 05-02 left session→chat resolution incomplete, add small `SessionChatMap` (or methods on `PendingInteractiveState`) updated whenever registry resolves an active `sessionId` for a `chatId` (e.g. after `getOrCreateDefault`, successful switch, new session) so SSE can find `chatId` for **D-11** delivery.
    - **MCP-02 / D-09:** In `makeMessageHandler`, before concurrency guard: if `pending.isAwaitingFreeTextAnswer(chatId)` and message is plain text (not a command — grammY routes commands first per **D-08**), then `postQuestionReply` with appropriate `answers` shape for open-ended flow, `clear(chatId)`, return. Do not start a new streaming turn for that message.
    - Preserve existing behavior for normal messages when not awaiting.
    - Edge: if awaiting but `manager.isBusy(chatId)` — define policy (likely block with “busy” or clear awaiting — document minimal behavior).
  </action>
  <acceptance_criteria>
    - `grep -n "awaiting\|AwaitingFreeText\|isAwaiting" src/bot/handlers/message.ts`
    - `grep -n "postQuestionReply" src/bot/handlers/message.ts`
    - `npm test` or `npx vitest run src/bot/handlers/message.test.ts` passes once tests added
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/bot/handlers/message.test.ts</automated>
  </verify>
  <done>Open-ended answers go to OpenCode; normal chat flow unchanged when not awaiting</done>
</task>

<task type="auto">
  <name>Task 2: Clear pending on cancel, switch, new + tests</name>
  <files>src/bot/handlers/cmd-cancel.ts, src/bot/handlers/cmd-switch.ts, src/bot/handlers/cmd-new.ts, src/bot/handlers/cmd-cancel.test.ts, src/bot/handlers/cmd-switch.test.ts, src/bot/handlers/cmd-new.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/src/bot/handlers/cmd-cancel.ts
    - /Users/admin/repos/opencode-telegram-client/src/bot/handlers/cmd-switch.ts
    - /Users/admin/repos/opencode-telegram-client/src/bot/handlers/cmd-new.ts
  </read_first>
  <action>
    - Inject `PendingInteractiveState` into `createBot` and these handlers (same pattern as registry/manager).
    - **MCP-06 / D-08:** `makeCmdCancelHandler`: call `pending.clear(chatId)` (or equivalent) **after** or **with** abort cleanup so interactive prompts are dismissed when user cancels streaming or pending work.
    - **MCP-06:** `makeCmdSwitchHandler`: on successful `switchTo`, call `pending.clear(chatId)` — session change invalidates old prompts per roadmap.
    - **MCP-06:** `makeCmdNewHandler`: after successful `createNamed` (active session changed), call `pending.clear(chatId)`.
    - Add or extend **`cmd-switch.test.ts`** and **`cmd-new.test.ts`**: mock `PendingInteractiveState` and assert **`clear(chatId)`** (or `clear` on the pending instance) is invoked on successful switch/new when pending existed (mirror `cmd-cancel.test.ts` pattern).
    - Optional UX: short ℹ️ reply if clearing had a visible prompt (Claude’s discretion — keep minimal).
  </action>
  <acceptance_criteria>
    - `grep -n "pending\\.clear\|clear(chatId)" src/bot/handlers/cmd-cancel.ts src/bot/handlers/cmd-switch.ts src/bot/handlers/cmd-new.ts`
    - Tests mock pending and assert `clear` called on cancel when pending existed
    - `cmd-switch.test.ts` and `cmd-new.test.ts` each contain an assertion that `clear` runs when pending was set (or equivalent spy)
    - `npx vitest run src/bot/handlers/cmd-cancel.test.ts src/bot/handlers/cmd-switch.test.ts src/bot/handlers/cmd-new.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/bot/handlers/cmd-cancel.test.ts src/bot/handlers/cmd-switch.test.ts src/bot/handlers/cmd-new.test.ts</automated>
  </verify>
  <done>All three handlers clear interactive state on session change or cancel</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests + full suite</name>
  <files>src/bot/handlers/message.test.ts, src/opencode/interactive-pending.test.ts</files>
  <read_first>
    - /Users/admin/repos/opencode-telegram-client/.planning/phases/05-mcp-questions-permissions/05-RESEARCH.md
  </read_first>
  <action>
    - Add test: while `awaitingFreeText`, a message starting with `/switch` is **not** handled by message handler as answer — verify via grammY test update routing or unit-test pending + handler split (D-08).
    - Add test: `question.replied` SSE clears pending (if unit-level on state module).
    - Run full `npx vitest run`; fix regressions.
    - Align any duplicate `clear` calls between SSE and commands idempotently (calling clear twice safe).
  </action>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - At least one test file references `D-08` or “command precedence” in describe/it name or comment
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Phase 5 automated gate green</done>
</task>

</tasks>
