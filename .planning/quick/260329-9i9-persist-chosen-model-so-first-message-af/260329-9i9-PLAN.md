# Quick Task 260329-9i9: Persist chosen model for first message after bot restart

## Tasks

1. **Persist last chosen model** — Add `src/persist/last-model.ts`: read/write JSON under `data/last-model.json`, track in-memory apply state so we do not spam PATCH when already synced.
2. **Save on `/model` success** — After successful `patchConfig` in `cmd-model.ts`, call `savePersistedModel(ref)`.
3. **Apply before first prompt** — In `message.ts`, `await ensurePersistedModelApplied(openCodeUrl)` before `sendPromptAsync`.
4. **Tests + gitignore** — Unit tests for persist module; add `data/` to `.gitignore`.
