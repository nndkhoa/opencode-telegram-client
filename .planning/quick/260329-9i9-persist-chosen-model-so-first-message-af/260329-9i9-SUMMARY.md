# Quick Task 260329-9i9 — Summary

## Done

- Added `src/persist/last-model.ts`: writes `data/last-model.json` after successful `/model` (and numeric index), optional `TELEGRAM_LAST_MODEL_FILE` override.
- `ensurePersistedModelApplied(openCodeUrl)` runs before `sendPromptAsync` so after a bot process restart the first user message re-`PATCH`es OpenCode with the last saved model (once per process per stored value, matching in-memory apply state after `/model`).
- Ignored `data/` in `.gitignore`.

## Verification

- `npm test`, `npm run typecheck`

## Git

Implementation is in the same commit as this quick-task documentation; use `git log -1 --oneline` for the hash.
